#ifndef IE_TOKENIZER_H_
#define IE_TOKENIZER_H_

/**
 * @file ie_tokenizer.h
 * @brief Thin wrapper to avoid duplicate type/decl definitions.
 *
 * The tokenizer public API lives in @ref ie_io.h. This header exists
 * only for historical include paths and re-exports that API.
 */

#include "ie_io.h"

#endif /* IE_TOKENIZER_H_ */
