/**
 * @file ie_tensor.c
 * @brief Minimal tensor utilities implementation (baseline).
 *
 * Note: Vector helpers are inline in the header (ie_tensor.h) for simplicity.
 * This TU exists to keep the module structure consistent and Doxygen-friendly.
 */

#include "ie_tensor.h"

/* No out-of-line symbols are required at this stage. */
