/* ========================================================================== */
/* File: engine/src/ie_tensor.c                                               */
/* ========================================================================== */
/**
 * @file ie_tensor.c
 * @brief Minimal tensor utilities implementation (baseline).
 *
 * This translation unit currently provides an out-of-line home for
 * documentation and future extensions to the tensor utilities declared
 * in @ref ie_tensor.h. Vector helpers may be inline in the header for
 * simplicity and performance; this TU exists to keep the module structure
 * consistent and Doxygen-friendly.
 *
 * @section design_notes Design notes
 * - Keep the API surface minimal and header-inline where it makes sense.
 * - Add out-of-line functions here only when they require a definition with
 *   internal linkage, platform-specific code, or non-trivial control flow.
 */

#include "ie_tensor.h"

/* At present, no out-of-line symbols are required. Placeholders only. */

/* ========================================================================== */
/* End of file                                                                */
/* ========================================================================== */
